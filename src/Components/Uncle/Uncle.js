import React from 'react';

const Uncle = ({ house }) => {
    return (
        <div>
            <h1>This is uncle</h1>
            <h4>House: {house}</h4>
        </div>
    );
};

export default Uncle;